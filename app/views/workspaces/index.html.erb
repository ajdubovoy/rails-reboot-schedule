<h1>All workspaces</h1>
<p>You can work at any of these gorgeous offices.</p>

<ul>
  <% @workspaces.each do |workspace| %>
    <li><%= link_to workspace.name, workspace_path(workspace) %> (<%= workspace.address %>)</li>
  <% end %>
</ul>
